# 스마트 제조 AI Agent (재개발) — CLAUDE.md

> DACON 스마트 제조 AI 해커톤 결과물의 개념을 계승하여 프로덕션 수준으로 재개발하는 프로젝트
> 기존 코드는 가져오지 않음 — 개념만 계승, 코드는 새로 작성

---

## 프로젝트 정체성

DACON 해커톤에서 RAG 기반 멀티에이전트로 본선 진출한 스마트 제조 AI Agent의 **개념을 계승**하되, 실제 동작하는 프로덕션 수준 시스템으로 **새로 구현**한다.

### 출발점 (중요)
- 현재 가진 것: **MVP 기획서 1부** (`docs/mvp-plan.md`)
- **기획서의 가정조차 실제 작동이 검증되지 않은 상태**
- 따라서 이 프로젝트는 "기획서대로 구현"이 아니라 **"기획 가정을 먼저 검증하고, 검증 통과한 것만 본 구현"**하는 검증 우선 프로젝트

### 핵심 작업 원칙
1. 기획서의 모든 기술적 가정은 **검증 대상**이지 기정사실이 아니다
2. 본 구현(`src/`) 전에 검증(`experiments/`)을 거친다
3. 검증 결과(성공·실패 모두)는 `docs/validation/`에 기록한다

---

## 폴더 구조와 작업 흐름

```
smart-mfg-ai-agent/
├── CLAUDE.md                  # 이 파일
├── README.md
├── .gitignore
├── .env.example               # 환경변수 템플릿 (실제 .env는 git ignored)
│
├── docs/
│   ├── mvp-plan.md            # MVP 기획서 (검증 대상)
│   ├── validation/            # 기획 가정 검증 기록 (성공·실패 모두)
│   └── decisions/             # ADR (아키텍처 의사결정 기록)
│
├── experiments/               # 기획 가정 검증용 PoC 코드 (버려도 되는 코드)
│
└── src/                       # 검증 통과한 것만 정식 구현
```

### 검증 우선 작업 흐름

```
기획서의 가정 (예: "Cross-Reference RAG로 문서 교차검증이 정확도를 높인다")
   ↓
experiments/ 에서 PoC로 빠르게 검증 ("진짜 되나? 효과 있나?")
   ↓
┌─ 성공 → docs/validation/ 에 "검증됨" 기록 → src/ 에 정식 구현
└─ 실패 → docs/validation/ 에 "왜 안 되는지 + 대안" 기록 → 기획 수정
```

**experiments/와 src/ 구분**: experiments는 빠르게 짜고 버리는 검증용. src는 검증 통과 후 제대로 짜는 본 구현. experiments의 코드를 src로 옮길 때는 그대로 복사하지 말고 프로덕션 품질로 재작성.

---

## 기술 스택

### 확정
- **언어**: Python 3.11+
- **에이전트 오케스트레이션**: LangGraph (멀티에이전트 그래프)
- **LLM 연동**: LangChain
- **RAG**: vector store + retriever (구체 구현은 검증 후 결정)

### 검증·설계 후 결정
- Vector DB (Chroma / FAISS / Qdrant 등 — 검증으로 선택)
- LLM 제공자 (OpenAI / Anthropic 등)
- 임베딩 모델
- 서빙 방식 (API 서버 필요 시 — 프로덕션 단계에서)

스택 추가 시 `docs/decisions/`에 선택 근거 ADR 기록. 새 의존성은 사전 제안.

---

## 절대 원칙 (Hard Constraints)

### 1. 검증되지 않은 가정을 사실로 다루지 않음

- 기획서에 적힌 기술적 주장(정확도 향상, 특정 기법의 효과 등)은 **검증 전까지 가설**이다
- "기획서에 그렇게 적혀있으니 맞다"는 추론 금지
- 구현 제안 시 "이건 아직 검증 안 된 가정"임을 명시

### 2. 정직 평가 (자소서·면접 어필 관련 — 최우선 가드레일)

이 프로젝트는 RAG·멀티에이전트가 핵심이므로, AI 기술을 다루는 것 자체는 과장이 아니다. 단 다음을 정직하게 **구분**한다:

- **"구현·검증했다" vs "개념을 적용해봤다"를 명확히 구분** — 자소서에서 무게가 다름
- 검증 없이 "정확도 X% 향상" 같은 수치 주장 금지 → 실제 측정한 것만
- "Cross-Reference RAG", "Safety Agent" 같은 명칭을 만들어 붙일 때, 실제 동작과 명칭이 부풀려 일치하지 않는지 점검
- LangGraph·LangChain을 "썼다"와 "깊이 이해하고 설계했다"를 구분
- 프로덕션 경험이 없는 부분(배포·스케일·모니터링 등)을 했다고 과장 금지

검증·구현 기록(`docs/validation/`, `docs/decisions/`)이 곧 정직한 어필의 근거가 된다.

### 3. 보안 — 시크릿·데이터 처리

- API 키(OpenAI, Anthropic 등)·제조 데이터는 **절대 하드코딩 금지** → 환경변수
- `.env`는 git ignored, `.env.example`에 키 이름만 (값 없이)
- 제조 도메인 데이터에 민감 정보가 있으면 별도 관리, repo에 커밋 금지
- GitHub 공개 repo 가능성 있으므로 커밋 전 시크릿 노출 점검

### 4. 공동/단독 작업 안전

- 명시되지 않은 파일·폴더 수정 금지
- 새 의존성 추가 시 사전 제안 (pip install 자동 실행 금지)
- 큰 작업·아키텍처 결정은 cc-cmds 워크플로우 활용 (아래)

---

## cc-cmds 플러그인 활용

이 프로젝트는 코드베이스를 새로 만드는 프로덕션 프로젝트라 cc-cmds가 적합하다.

```
# 기획서 가정 검증 (가장 먼저 — 기획서의 모순·미흡 발견)
/cc-cmds:design-review docs/mvp-plan.md

# 검증 필요한 영역의 설계 토론
/cc-cmds:design "<주제 — 예: 'RAG 문서 교차검증 파이프라인 구조'>"

# 빠른 검증·방향 설정
/cc-cmds:design-lite "<주제>"

# 설계 확정 후 단계적 구현
/cc-cmds:implement docs/<설계문서>.md "<범위>"

# 코드 리뷰
/cc-cmds:review [target]
/cc-cmds:review-lite [target]
```

**권장 진입 순서**: `design-review docs/mvp-plan.md`로 기획서를 먼저 점검 → 발견된 가정을 `experiments/`에서 검증 → 통과분을 `design`으로 설계 → `implement`로 구현.

cc-cmds 선결 조건(CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS)은 설정 완료됨. 완료 알림은 Windows라 미작동.

---

## 응답 톤 및 작업 방식

### 톤
- 직설·구조화·근거 기반. 위로·완곡어 최소화
- 결론 → 근거 순서. hedging 표현 지양
- 검증 안 된 것을 "된다"고 단정하지 않음

### 작업 진행
- 기획 가정에 대한 작업 시작 전 "이건 검증 대상"임을 환기
- 새 기능·아키텍처는 cc-cmds 또는 ADR 작성 권유
- 명시되지 않은 파일 수정 금지, 새 의존성 사전 제안
- 실시간 정보(라이브러리 최신 버전·API 변경 등)가 필요하면 WebSearch로 조회

---

## Git 운용

- GitHub repo (공개/비공개 관리)
- 커밋 단위: 한 가지 의도 = 한 커밋
- experiments/ 커밋은 "검증용 PoC"임을 메시지에 명시
- 명시 요청 없이 `git commit`·`git push`·`git reset` 자동 실행 금지
- 커밋 전 시크릿 노출 점검 (.env, API 키)

---

## 권장 세션 설정

- 모델: Opus 4.8 / effort: xhigh / 적응형 사고: ON
- 코딩·에이전트 작업이라 공식 권장 xhigh. cc-cmds 설계·구현 사이클이 장시간 작업
- 검증·탐색 위주의 가벼운 작업은 high로 낮춰도 됨
- 이 설정은 세션 시작 시 UI에서 수동 선택 (자동 적용 아님)

---

## 작업 환경

- OS: Windows + Docker Desktop (Hyper-V 백엔드), WSL2 비활성
- 에디터: VS Code
- 명령어 제안 시 Windows 환경 우선
- Python 가상환경 사용 (venv 권장)

---

## 응답 시 절대 안 하는 것

1. **검증 안 된 기획 가정을 사실로 구현** — 검증 먼저
2. **측정 안 한 성능 수치 주장** — "정확도 향상" 등은 실측만
3. **시크릿 하드코딩** — 환경변수로
4. **명시 안 된 파일 수정 / 무단 의존성 추가**
5. **AI 기술 과장** — 구현한 것과 개념만 아는 것 정직 구분
6. **추상어 결론 / AI 특유 균형 표현** — "~뿐만 아니라 ~까지" 등 금지

---

## 갱신 이력

- 2026-06-04: 초안 작성 (검증 우선 구조, 개념 계승·코드 신규 작성 방침)
