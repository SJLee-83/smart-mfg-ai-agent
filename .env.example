# =====================================================
# 환경변수 템플릿 — 실제 값은 .env에 (git ignored)
# =====================================================

# LLM API 키 (사용하는 것만 채우기)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Vector DB 설정 (검증 후 선택)
# VECTOR_DB_PATH=./chroma_db

# 기타 설정
# LOG_LEVEL=INFO
